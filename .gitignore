# ==========================================
# NAVAPE FULL-STACK .GITIGNORE
# ==========================================

# 1. DEPENDENCIES (Never push these, they are massive)
node_modules/
package-lock.json
yarn.lock

# 2. ENVIRONMENT VARIABLES & SECRETS (CRITICAL)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# 3. FRONTEND BUILD ARTIFACTS (Vite output)
dist/
dist-ssr/
build/
out/

# 4. LOGS (NPM & Runtime logs)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 5. OS GENERATED FILES (Mac/Windows junk files)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 6. EDITOR & IDE WORKSPACES (Keeps repo clean for other devs)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?